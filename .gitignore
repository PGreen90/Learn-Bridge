node_modules
dist
dist-ssr
*.tsbuildinfo
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
.claude/*
!.claude/commands/
!.claude/skills/

# Källmaterial (PDF:er) hör inte hemma i kod-repot – för stora/onödiga
*.pdf

# Systemrevisorns mätrapporter (docs/systemrevisorn.md) – genererade, ej kod
revisor-output/

# Beslut B etapp 2: buntade serverfunktioner (api-src/*.ts → api/*.js).
# Genererade av scripts/build-api.mjs; Vercel återskapar dem via `npm run build`.
# Handskrivna api/*.ts (t.ex. health.ts) versionshanteras som vanligt.
/api/*.js
